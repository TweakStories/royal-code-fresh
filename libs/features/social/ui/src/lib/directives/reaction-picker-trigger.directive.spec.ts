import { ReactionPickerTriggerDirective } from './reaction-picker-trigger.directive';

describe('ReactionPickerTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new ReactionPickerTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
